import Vuex from 'vuex';
import Vue from 'vue';

Vue.use(Vuex);

export const state = {};
export const mutations = {};

export const store = new Vuex.Store({
  state,
  mutations,
});

export default store
